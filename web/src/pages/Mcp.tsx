import { EmptyState } from '../components/core/index.js';

/** Stub — replaced by MCP manager (wmc.8). */
export function Mcp() {
  return (
    <main className="layout-main page">
      <section className="page__section">
        <EmptyState instruction="MCP manager (wmc.8) · page pending" />
      </section>
    </main>
  );
}
