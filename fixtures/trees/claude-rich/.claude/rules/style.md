# Style rules

- Prettier is the formatter; never hand-format.
- Imports: node builtins, then packages, then local — separated by blank lines.
- No default exports outside React components (we have none here).
- Error messages are sentences without trailing periods.
