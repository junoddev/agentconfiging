/**
 * Backwards-compatible runtime table.
 *
 * Canonical runtime knowledge lives in AgentProfile records. This module keeps
 * the historical import path while projecting those profiles to RuntimeFormat.
 */
export { RUNTIME_FORMATS } from '../profiles/projection.js';
